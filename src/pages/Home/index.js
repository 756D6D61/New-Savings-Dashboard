import React from 'react'
import Nav from '../../components/Nav'
import NavTest from '../../components/NavTest'

const Home = ({ route }) => {
	return (
		<div>
			<NavTest route={route} />
		</div>
	)
}

export default Home
